{
  sort = 
  {
  }
  markdup = 
  {
  }  
  pileup = 
  {
  }
  defPart = 
  {
  }

  preprocessorNames = ( sort , markdup );
  preprocessorAlgorithms = ( sortReads , markDuplicates );

  partitionerNames = ( defPart );
  partitionerAlgorithms = ( defaultPartitioner );

  callNames = ( pileup );
  callAlgorithms = ( SimpleSNP );

  postprocessorNames = ( );
  postprocessorAlgorithms = ( );
}
