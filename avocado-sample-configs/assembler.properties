{
  AlignedReads =
  {
  }

  assembler = 
  {
    lowCoverageTrimmingThreshold = 0.1
  }
  defPart = 
  {
  }

  preprocessorNames = ( );
  preprocessorAlgorithms = ( );

  partitionerNames = ( defPart );
  partitionerAlgorithms = ( defaultPartitioner );

  callNames = ( assembler );
  callAlgorithms = ( AssemblyPhaser );

  postprocessorNames = ( );
  postprocessorAlgorithms = ( );
}
