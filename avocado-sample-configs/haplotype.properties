{
  AlignedReads =
  {
  }

  haplotyper = 
  {
    haplotypeAligner =
    {
      LOG_GAP_OPEN = -4.0;
      LOG_GAP_CONTINUE = -2.0;
      LOG_SNP_RATE = -3.0;
      LOG_INDEL_RATE = -4.0;
      LOG_PADDING_PENALTY = -0.0;
    }

    readAligner =
    {
      LOG_GAP_OPEN = -100.0;
      LOG_GAP_CONTINUE = -100.0;
      LOG_SNP_RATE = -1.7;
      LOG_INDEL_RATE = -100.0;
      LOG_PADDING_PENALTY = -0.0;
    }
  }
  defPart = 
  {
  }
  coalesce =
  {
    partitions = 12
  }

  preprocessorNames = ( coalesce );
  preprocessorAlgorithms = ( coalesceReads );

  partitionerNames = ( defPart );
  partitionerAlgorithms = ( defaultPartitioner );

  callNames = ( haplotyper );
  callAlgorithms = ( CallHaplotypesFromReads );

  postprocessorNames = ( );
  postprocessorAlgorithms = ( );
}
