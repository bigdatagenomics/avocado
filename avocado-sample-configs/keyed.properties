{
  KeyedReads = {
    normalFileName = ../resources/NA12878_pre_normal.sorted.adam
  }
  readExplorer = {
  }
  mutectGenotyper = {
    normalId = normal;
    somaticId = tumor;
  }
  nonRef = {
  }

  mapping_quality_filter = {
  }
  mate_rescue_filter = {
  }
  duplicateFilter = {
  }
  inputStage = KeyedReads;

  preprocessorNames = (mate_rescue_filter, duplicateFilter);
  preprocessorAlgorithms = (MateRescueFilter,DuplicateFilter);

  explorerName = readExplorer;
  explorerAlgorithm = ReadExplorer;

  genotyperName = mutectGenotyper;
  genotyperAlgorithm = MutectGenotyper;

  postprocessorNames = ( nonRef );
  postprocessorAlgorithms = ( filterReferenceCalls );
}
